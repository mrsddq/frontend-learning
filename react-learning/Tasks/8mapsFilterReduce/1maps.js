const numbers = [1, 2, 3, 4];
const doubled = numbers.map(num => num * 2);
console.log(doubled); // Output: [2, 4, 6, 8]

// Explanation: Here, map multiplies each element in the numbers array by 2 and returns a new array [2, 4, 6, 8].